# Study Library Host Follow-Up

- The Study Library host should suppress or visually distinguish an inverse “Used in Vocabulary” link when a single-Kanji lexical record has exactly the same label as its required `spelling` target. The module must retain that authored word-to-Kanji relationship for composition integrity, so removing it here would hide the structural link rather than solve the host navigation loop.
